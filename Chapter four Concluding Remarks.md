### 4. 结语 Concluding Remarks

硬件预取作为学术研究和工业发展的主题已经有超过40年的历史。然而，由于持续的扩展趋势不断拉大处理器性能与内存访问延迟之间的差距，硬件预取的重要性以及隐藏内存系统延迟的需求只增不减——进一步的创新仍然至关重要。


在这篇入门读物中，我们调研了已经开发的各种预取技术，并突出了这些技术所基于的主要程序行为原则。我们希望这本书能作为该领域的入门介绍，作为对大量关于硬件预取文献的概览，并作为一个促进新研究工作的催化剂。

未来的工作中仍有许多挑战需要解决。指令获取仍然是一个基本瓶颈，特别是在拥有复杂软件栈和日益增长的片上指令工作集的服务器中。尽管指令足迹通常可以完全适配于大型末级缓存，但周期时间限制对L1指令缓存容量施加了严格的限制，而对更大缓存的访问延迟依然存在。近年来，针对指令流的高级提议已经实现了即使在复杂软件栈的存在下也能达到惊人的准确性和覆盖度（>99.5%）。这些提案更广泛采用的关键在于将片上元数据存储减少到实用水平的技术。

指令预取面临的另一个关键挑战是编程语言和软件工程的发展，这往往使我们讨论的技术变得复杂甚至失败。面向对象编程实践、动态调度和托管运行时都导致频繁、短小函数调用的使用增加，通过函数指针进行间接寻址，寄存器间接分支和多路控制转移。动态代码生成/优化、解释型语言和即时编译导致程序的控制结构可能被掩盖且指令地址随时间变化。虚拟化和操作系统层同样因为经常出现的虚拟机退出和特权功能模拟陷阱而使控制流复杂化和模糊化。

在硬件方面，处理器越来越支持多个并发硬件线程，这些线程必须共享已过度订阅的指令缓存容量。预取器必须得到增强以在线程间共享有限的容量和带宽，区分每个线程发出的指令流，并考虑预取策略与线程优先级/提取策略之间的交互。

对于数据预取而言，一个关键的遗留挑战是在广泛的负载范围内保持低准确性和覆盖度。随着数据密集型工作负载的出现和大规模内存内数据服务的增长，对有效数据预取的需求越来越大，但是内存容量的增长使得即使是本入门书中涵盖的最先进的基于历史的预取技术，在减少重复历史模式和元数据存储要求方面也显得力不从心。未来的数据预取进步需要捕捉具有更低元数据存储需求和更高准确性的重复访问模式。

图形处理单元和其他形式的专业加速器也开始出现预取技术，它们可能具有与传统处理器明显不同的代码和数据访问模式。就图形处理器而言，内存访问停滞和线程/线程束调度以复杂的方式相互作用，为协同设计创造了新的机会。

过去十年中浮现的一个基本挑战是，由于Dennard缩放定律的放缓[130, 131]和供电电压的平稳，功耗已成为首要约束条件。一方面，预取器消除了停顿，这可以通过更有效地利用硬件资源来提高能源效率。另一方面，大多数预取器都需要辅助硬件结构，这需要消耗能量。此外，预取器经常获取错误的数据块，这可能会浪费大量能量。实际上，许多简单（但广泛应用）的设计非常不准确；它们检索的超过一半的数据块可能永远不会被访问。预取技术的进步必须将能源效率作为评估预取器设计有效性的一级约束，同时结合其他关键指标如准确性和覆盖度。